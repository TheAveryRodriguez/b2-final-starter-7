<%= render partial: "shared/nav" %>
<br>

<section id="bulk-discount-info"
  <h3> Bulk Discount ID: <%= "#{@bulk_discount.id}" %></h3>
    <p>Quantity Threshold: <%= @bulk_discount.quantity_threshold %></p>
    <p>Percentage Discount: <%= @bulk_discount.percent_discount %></p>
</section>
<br>